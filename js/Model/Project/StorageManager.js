function ProxyStorage(storages) {
	this.storage = storages;
	this.funs = {};
}


function removeRessource(ressource, argsFromRessource, storage) {
	var argss = argsFromRessource(ressource);
	argss.map(function (args) {
		remove
	})
}

StorageManager.prototype._addFunction = function(fname, hasChanged, isResultOf) {
};

StorageManager.prototype._updateRessource = function(ressource) {
};